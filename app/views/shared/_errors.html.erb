<% if @movie.errors.any? %>
  <section>
    <h2>
      The <%= @movie.class.name.titleize.downcase %> could not be saved.
    </h2>
    <h3>
      Please correct the
      <%= pluralize(@movie.errors.count, 'error') %>:
    </h3>
    <ul>
      <% @movie.errors.full_messages.each do |error| %>
        <li><%= error %></li>
      <% end %>
    </ul>
  </section>
<% end %>
